package training;

public class MainTest {
    private static int[] arr ={100};
    public static void main(String[] args) {

        //CriticalTest criticalTest = new CriticalTest();
        //System.out.println(criticalTest.find_min(arr));

        /*
        ShapeInterface rectangle = new Rectangle();
        ShapeInterface triangle = new Triangle();

        ShapeAbstract rectangle1 = new Rectangle();
        ShapeAbstract triangle1 = new Triangle();

        rectangle.name();
        rectangle.message();
        System.out.println(rectangle.getArea());
        triangle.name();
        triangle.message();
        System.out.println(triangle.getArea());
        */
        /*
        String words = "es mesmo gatinha";
        String[] wordCount;

        for (int i = 0; i < words.length(); i++) {
            wordCount = words.split(" ");
            System.out.println(wordCount);
        }


        String a = "ola";

        if (a.matches("^[a-zA-Z]$")){
            System.out.println("String ok");
        }
        else {
            System.out.println("String not ok");
        }

        */
    }
}
